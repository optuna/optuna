name: Tests

on:
  push:
    branches:
      - master
  pull_request: {}
  schedule:
    - cron: '0 23 * * SUN-THU'

jobs:
  tests:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: [3.6, 3.7, 3.8, 3.9]

    services:
      redis:
        image: redis
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    # Not intended for forks.
    if: github.repository == 'optuna/optuna'

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Setup Python${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

    - name: Setup cache
      uses: actions/cache@v2
      env:
        cache-name: test
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-${{ matrix.python-version }}-${{ env.cache-name }}-${{ hashFiles('**/setup.py') }}-v1
        restore-keys: |
          ${{ runner.os }}-${{ matrix.python-version }}-${{ env.cache-name }}-${{ hashFiles('**/setup.py') }}

    # TODO(c-bata): Remove this step after fixing the version constraint of sklearn.
    - name: Install Cython for scikit-learn installation which does not provide Python 3.9 wheel.
      run: pip install --use-deprecated=legacy-resolver --progress-bar off Cython
      if: matrix.python-version == '3.9'

    - name: Install
      run: |
        python -m pip install --upgrade pip
        pip install --use-deprecated=legacy-resolver --progress-bar off -U setuptools

        # Install minimal dependencies and confirm that `import optuna` is successful.
        pip install --use-deprecated=legacy-resolver --progress-bar off .
        python -c 'import optuna'
        optuna --version

        pip install --use-deprecated=legacy-resolver --progress-bar off .[tests]
        pip install --use-deprecated=legacy-resolver --progress-bar off .[optional]

        # Required for samplers_tests/test_samplers.py.
        # TODO(hvy): Fix samplers_tests/test_samplers.py to not require optional dependencies and remove these installs.
        pip install --use-deprecated=legacy-resolver scikit-optimize
        pip install --use-deprecated=legacy-resolver cma

    # TODO: Add the deselected tests back after fixing version constraint of sklearn.
    - name: Tests
      run: |
        if [ ${{ matrix.python-version }} = 3.9 ]; then
          pytest tests --ignore tests/integration_tests \
            --deselect tests/samplers_tests/test_samplers.py::test_nan_objective_value \
            --deselect tests/samplers_tests/test_samplers.py::test_partial_fixed_sampling
        else
          pytest tests --ignore tests/integration_tests
        fi
